name: deploy-ww-api

on:
  push:
    branches:
      - master
    paths:
      - 'cmd/ww-srv/**'
      - 'internal/**'
      - 'pkg/**'
      - 'go.mod'
      - 'go.sum'
  workflow_dispatch:

jobs:
  deploy-ww-api:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (
        !contains(github.event.head_commit.message, 'ww-ui/') &&
        !contains(github.event.head_commit.message, 'from sonastea/master')
      )
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Trigger Coolify Deployment for API
        run: |
          curl --request GET "${{ secrets.COOLIFY_WW_API_WEBHOOK }}" --header "Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}"
